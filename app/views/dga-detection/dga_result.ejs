<html>
<head>
	<%- include('../common/imports.ejs') %>
	<title>DGA Detection | Result</title>
</head>
<body>
	<%- include('../common/header.ejs') %>
	Result
	<br><br>
	<% if(data.status == 200) { %>
		<% Object.keys(data.result).forEach(function(key) { %>
	  		<%= key %> - <%= data.result[key] %> <br>
		<% }) %>
	<% } else { %>
		Errore <%= data.status %>: <%= data.text %>
	<% } %>
</body>
</html>